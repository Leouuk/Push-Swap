/*
** EPITECH PROJECT, 2021
** my_compute_square_root
** File description:
** Compute square root
*/

int my_compute_square_root(int nb)
{
    int i = 0;
    int result = 0;

    if (result <= 0 || nb == 0)
        return (84);
    while (i != nb && result < nb) {
    }
    return (i);
}
